import 'package:flutter/material.dart';

class AppColors {
  // الألوان الأساسية
  static const Color primary = Color.fromARGB(255, 68, 118, 154);
  static const Color secondary = Color.fromARGB(255, 196, 182, 220);
  
  // ألوان الخلفيات والبطاقات
  static const Color background = Colors.white;
  static const Color cardBackground = Color.fromARGB(255, 229, 229, 233);
  static const Color tileBackground = Color(0xFFE0E0E0); // Colors.grey[300]
  
  // ألوان النصوص
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.white;
  
  // ألوان الحالات (Success, Error)
  static const Color success = Colors.green;
  static const Color error = Colors.red;
}
